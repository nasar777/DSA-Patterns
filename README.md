# DSA-Patterns
substring with same length->sliding window
sliding window fixed size k pattern

int l=0;
for(int r=0;r<n;r++){
            expand r
            check condition
                 shrink l
            update answer
            
}
